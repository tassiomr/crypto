const defaultState = {
  coinsList: [],
  isLoading: false,
  coinDetails: undefined,
};

export default (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
